import 'package:flutter/material.dart';

class GestionJornadas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Hola Mundo'),
    );
  }
}
